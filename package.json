{
  "name": "game-store-monorepo",
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  }
}
